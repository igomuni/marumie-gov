# GitHub CLIを使ったリリース作成手順

**作成日時**: 2025-11-15 08:19
**目的**: GitHub CLIを使ってリリースを自動作成し、データファイルをアップロードする

---

## 前提条件

### データファイルの準備

現在のプロジェクトから `projects_data.tar.gz` を作成：

```bash
cd ./public/data
tar -czf ./projects_data.tar.gz projects
ls -lh ./projects_data.tar.gz
# 出力: 5.1M のファイルサイズを確認
```

---

## GitHub CLIのインストール

### macOSの場合

```bash
# Homebrewを使ってインストール
brew install gh

# インストール確認
gh --version
```

### その他のOS

- **Windows**: [GitHubリリースページ](https://github.com/cli/cli/releases)からインストーラーをダウンロード
- **Linux**:
  ```bash
  # Debian/Ubuntu
  sudo apt install gh

  # Fedora/RHEL
  sudo dnf install gh
  ```

---

## GitHub CLIの初期設定

### 1. GitHubにログイン

```bash
gh auth login
```

対話形式で以下を選択：
1. **What account do you want to log into?**: `GitHub.com`
2. **What is your preferred protocol for Git operations?**: `HTTPS` または `SSH`
3. **Authenticate Git with your GitHub credentials?**: `Yes`
4. **How would you like to authenticate GitHub CLI?**: `Login with a web browser`

ブラウザが開くので、GitHubにログインして認証コードを入力

### 2. 認証確認

```bash
gh auth status
```

出力例：
```
✓ Logged in to github.com as igomuni
✓ Git operations protocol: https
✓ Token: gho_****
```

---

## リリースの作成（GitHub CLI）

### 方法1: 対話形式で作成

```bash
cd .

gh release create v1.0.0 \
  ./projects_data.tar.gz \
  --title "v1.0.0 - Initial Data Release" \
  --notes "$(cat <<'EOF'
## 初回データリリース

行政事業レビューのプロジェクトデータ（前処理済み）

### 📊 含まれるデータ
- 事業別のプロジェクトJSON（12,573事業分）
- 対象年度: 2014年度〜2024年度
- ファイルサイズ: 5.1MB（圧縮後）

### 🚀 使用方法
このリリースのアセットは、ビルド時に自動的にダウンロードされます。

\`\`\`bash
npm run download-data
\`\`\`

### 📦 含まれるファイル
- \`projects_data.tar.gz\` - 前処理済みプロジェクトデータ（public/data/projects/配下の全JSONファイル）

### 🔗 関連情報
- 元データ: [行政事業レビュー](https://www.gyoukaku.go.jp/)
- データ処理パイプライン: [RS System Pipeline](https://github.com/igomuni/rs_system_pipeline)
EOF
)"
```

### 方法2: シンプルな作成

```bash
gh release create v1.0.0 \
  ./projects_data.tar.gz \
  --title "v1.0.0 - Initial Data Release" \
  --notes "行政事業レビューのプロジェクトデータ（12,573事業分、2014-2024年度）"
```

### 方法3: ファイルから説明文を読み込み

```bash
# リリースノートをファイルに保存
cat > /tmp/release-notes.md <<'EOF'
## 初回データリリース

行政事業レビューのプロジェクトデータ（前処理済み）

### 📊 含まれるデータ
- 事業別のプロジェクトJSON（12,573事業分）
- 対象年度: 2014年度〜2024年度
- ファイルサイズ: 5.1MB（圧縮後）

### 🚀 使用方法
このリリースのアセットは、ビルド時に自動的にダウンロードされます。

```bash
npm run download-data
```

### 📦 含まれるファイル
- `projects_data.tar.gz` - 前処理済みプロジェクトデータ

### 🔗 関連情報
- 元データ: [行政事業レビュー](https://www.gyoukaku.go.jp/)
- データ処理パイプライン: [RS System Pipeline](https://github.com/igomuni/rs_system_pipeline)
EOF

# リリース作成
gh release create v1.0.0 \
  ./projects_data.tar.gz \
  --title "v1.0.0 - Initial Data Release" \
  --notes-file /tmp/release-notes.md
```

---

## リリース作成の確認

### コマンドで確認

```bash
# リリース一覧を表示
gh release list

# 特定のリリースの詳細を表示
gh release view v1.0.0

# ブラウザでリリースページを開く
gh release view v1.0.0 --web
```

### Webで確認

https://github.com/igomuni/marumie-gov/releases/tag/v1.0.0

以下を確認：
- ✅ タグ `v1.0.0` が作成されている
- ✅ リリースタイトルが正しい
- ✅ 説明文が正しく表示されている
- ✅ `projects_data.tar.gz` がアセットとして添付されている
- ✅ ファイルサイズが 5.1MB であることを確認
- ✅ ダウンロードリンクが有効

---

## 推奨: ワンライナーでリリース作成

すべてを1つのコマンドで実行：

```bash
cd . && \
tar -czf ./projects_data.tar.gz -C public/data projects && \
gh release create v1.0.0 \
  ./projects_data.tar.gz \
  --title "v1.0.0 - Initial Data Release" \
  --notes "$(cat <<'EOF'
## 初回データリリース

行政事業レビューのプロジェクトデータ（前処理済み）

### 📊 含まれるデータ
- 事業別のプロジェクトJSON（12,573事業分）
- 対象年度: 2014年度〜2024年度
- ファイルサイズ: 5.1MB（圧縮後）

### 🚀 使用方法
```bash
npm run download-data
```

### 📦 含まれるファイル
- `projects_data.tar.gz` - 前処理済みプロジェクトデータ

### 🔗 関連情報
- 元データ: [行政事業レビュー](https://www.gyoukaku.go.jp/)
- データ処理パイプライン: [RS System Pipeline](https://github.com/igomuni/rs_system_pipeline)
EOF
)"
```

---

## リリース作成後の動作確認

### 1. データダウンロードのテスト

```bash
# 既存のデータを削除
rm -rf /Users/igomuni/MyGitHub/marumie-gov/public/data/projects

# ダウンロードスクリプトを実行
cd .
npm run download-data

# 結果確認
ls -lh public/data/projects/*.json | wc -l
# 期待値: 12573
```

### 2. ビルドテスト

```bash
npm run build
```

成功すれば、リリースが正常に機能していることが確認できます。

---

## その他の便利なコマンド

### リリースの編集

```bash
# リリースノートを編集
gh release edit v1.0.0 --notes "新しい説明文"

# タイトルを編集
gh release edit v1.0.0 --title "新しいタイトル"
```

### ファイルの追加・削除

```bash
# ファイルを追加
gh release upload v1.0.0 /path/to/newfile.txt

# ファイルを削除
gh release delete-asset v1.0.0 projects_data.tar.gz
```

### リリースの削除

```bash
# リリースを削除（タグは残る）
gh release delete v1.0.0

# リリースとタグを削除
gh release delete v1.0.0 --yes --cleanup-tag
```

---

## トラブルシューティング

### エラー: `gh: command not found`

GitHub CLIがインストールされていません。インストール手順に従ってください。

### エラー: `HTTP 401: Bad credentials`

認証が必要です：

```bash
gh auth login
```

### エラー: `release already exists`

同じタグのリリースが既に存在します：

```bash
# 既存のリリースを削除
gh release delete v1.0.0 --yes

# または、別のバージョン番号を使用
gh release create v1.0.1 ...
```

### エラー: `file not found`

ファイルパスが正しいか確認：

```bash
ls -lh ./projects_data.tar.gz
```

---

## まとめ

GitHub CLIを使えば、Webブラウザを使わずにコマンドラインだけで：
- ✅ リリースの作成
- ✅ ファイルのアップロード
- ✅ リリースノートの設定
- ✅ リリースの編集・削除

すべてが可能になります！

次のステップ: [GitHub開発ワークフロー実践手順](./20251115_0814_GitHub開発ワークフロー実践手順.md)のステップ3（Pull Request作成）に進んでください。
